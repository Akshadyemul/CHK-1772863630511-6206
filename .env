# Replace localhost with your computer's local IP address (e.g. 192.168.1.5) if you are testing on a physical device.
# Expo requires the EXPO_PUBLIC_ prefix for environment variables to be accessible in the app.
EXPO_PUBLIC_API_BASE_URL=http://localhost:5000
